#include <stdio.h>
/**
 * main - main funtion initialised
 * Description: 'my first C program'
 * Return: Always 0 (Success)
 */
int main(void)
{
char(msg) = "Programming is like building a multilingual puzzle";
puts(msg);
return (0);
}
